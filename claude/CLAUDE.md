# Global Claude Code Instructions

## Shell
Using fish shell. When showing shell commands for me to run interactively, prefer fish syntax (`set -gx`, `fish_add_path`, etc.). Claude Code's Bash tool still runs in bash — only affects examples shown to me.
